serverless:
  port: 7001

db:
  write:
    host: 0.0.0.0
    port: 3306
    user: dev
    password: anByU=9c77?cD26k
    logging: false
    database: openrecord
    synchronize: false
  setup:
    host: 0.0.0.0
    port: 3306
    user: root
    password: password
    database: mysql
    multipleStatements: true

log:
  level: info
  depth: 10
  console:
    enabled: true

aws:
  region: us-east-1
  securityGroupIds: []
  subnetIds: []
  ? domainName
  ? certificateArn

auth:
  jwtSecret: shhh
